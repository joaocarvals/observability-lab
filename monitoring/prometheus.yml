#Config gloval do Prometheus
global:
#Intervalo de coleta de métricas
  scrape_interval: 5s

scrape_configs:
  - job_name: "observability-api"

    static_configs:
      - targets:
          - host.docker.internal:8000